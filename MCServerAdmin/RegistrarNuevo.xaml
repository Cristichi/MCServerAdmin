<Page
    x:Class="MCServerAdmin.RegistrarNuevo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MCServerAdmin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2" Style="{ThemeResource HeaderTextBlockStyle}" Text="Registrar un Nuevo Jugador" HorizontalAlignment="Center"/>

        <TextBox x:Name="TxtNombre" Grid.ColumnSpan="2" Grid.Row="1" PlaceholderText="Nombre" FontSize="24" Width="600" Margin="16"/>

        <ComboBox x:Name="CBSkin" ItemsSource="{x:Bind cbItems}" Grid.ColumnSpan="2" Grid.Row="2" PlaceholderText="Skin" FontSize="24" Margin="16"/>

        <ComboBox x:Name="CBRango" ItemsSource="{x:Bind cbRangos}" Grid.ColumnSpan="2" Grid.Row="3" PlaceholderText="Rango" FontSize="24" Margin="16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Grid.Row="4" Margin="10" Click="Button_Click_Cancelar">
                <StackPanel Orientation="Horizontal">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;"/>
                    <TextBlock Text="Cancelar"/>
                </StackPanel>
            </Button>
            <Button Margin="10" Click="Button_Click_Registrar">
                <StackPanel Orientation="Horizontal">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE710;"/>
                    <TextBlock Text="Agregar"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
