<Page
    x:Class="MCServerAdmin.RegistrarNuevo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MCServerAdmin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2" Style="{ThemeResource HeaderTextBlockStyle}" Text="Registrar un Nuevo Jugador" HorizontalAlignment="Center" FontSize="100"/>

        <TextBox x:Name="TxtNombre" Grid.ColumnSpan="2" Grid.Row="1" PlaceholderText="Nombre" HorizontalAlignment="Center" HorizontalTextAlignment="Center" FontSize="60" Width="600"/>

        <ComboBox x:Name="CBSkin" ItemsSource="{x:Bind cbItems}" Grid.ColumnSpan="2" Grid.Row="2" PlaceholderText="Skin" HorizontalAlignment="Center" FontSize="60"/>

        <ComboBox x:Name="CBRango" ItemsSource="{x:Bind cbItems}" Grid.ColumnSpan="2" Grid.Row="3" PlaceholderText="Rango" HorizontalAlignment="Center" FontSize="60"/>

        <Button Grid.Row="4" HorizontalAlignment="Stretch" Margin="10" Click="Button_Click_Cancelar">
            <StackPanel Orientation="Horizontal">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;"/>
                <TextBlock Text="Cancelar"/>
            </StackPanel>
        </Button>
        <Button Grid.Column="1" Grid.Row="4" HorizontalAlignment="Stretch" Margin="10" Click="Button_Click_Registrar">
            <StackPanel Orientation="Horizontal">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE710;"/>
                <TextBlock Text="Agregar"/>
            </StackPanel>
        </Button>
    </Grid>
</Page>
